const array = [1, 2, 3]

array.forEach((item) => {
  console.log(item)
})

array.forEach((item) => {
  console.log(item)
})
const ok = true

try {
  // do something
} catch (error) {
  // handle error
}
